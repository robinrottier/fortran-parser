<Project>
<PropertyGroup>
	  <Version Condition="'$(RELEASE_VERSION)' != ''">$(RELEASE_VERSION)</Version>
	  <VersionPrefix Condition="'$(RELEASE_VERSION)' == ''">0.1.0</VersionPrefix>
	  <VersionSuffix Condition="'$(RELEASE_VERSION)' == ''">dev-$([System.DateTime]::UtcNow.ToString(`yyyyMMdd-HHmm`))</VersionSuffix>
</PropertyGroup>
</Project>